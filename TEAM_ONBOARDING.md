# BirdPilot 两位成员协作说明

这份说明给现在一起在线推进项目的两位成员用。先把仓库和小数据包跑明白，再一起做第一轮退化小样。

## 我们最终要做什么

BirdPilot 的目标是一台可放到野外的鸟类智能观测设备：摄像头取得图像，设备识别鸟种并保存观测记录。当前先在线完成模拟退化、模型对比和桌面设备闭环，真实野外观测放在最后验收。

当前已经有 Birds-525 上的 MobileNetV3 模型、ONNX 导出和 Mac 本地推理基准。尚未完成的核心工作是退化增强对照实验、RK3588S 板端推理和真实野外验证。

## 开始前先做这些

1. 阅读根目录 `README.md`，了解训练、导出和推理流程。
2. 创建 Python 环境并安装 `requirements.txt`。
3. 向项目负责人领取 `BirdPilot_starter_data_v1.zip`；完整数据先不用拿。
4. 解压后确认其中有 `train`、`valid`、`test` 三种划分、8 个鸟种和 `manifest.csv`。
5. 先查看图片、路径和标签，不要立刻修改模型或大批量生成退化图。
6. 这一轮只做退化小样，不要先扩到训练、部署或野外采集。

## 数据边界

- `train`：未来可用于退化增强训练。
- `valid`：用于选择退化强度、概率和训练方案。
- `test`：只用于最终比较，禁止加入训练，也不能根据它反复调参数。
- 新收集的外部图片：后续作为额外盲测或真实野外证据，不能混写成现有测试集结果。

这个小包只用来熟悉目录、看图和验证处理流程，不用它去报 524 类正式准确率。

## 模型文件如何领取

模型不放在公开 Git 仓库。通过项目负责人提供的团队共享目录领取，并保持以下文件成套对应：

- MobileNetV3 `.pt` 权重；
- 对应的训练配置；
- 524 类标签映射；
- 如果领取 ONNX，`.onnx` 与 `.onnx.data` 必须同时存在。

模型文件拿到后记一下文件名和版本即可；不要在不同群里反复转发副本。

## 你们的第一项共同任务

你们一起把“小规模模拟退化测试”跑通，但别同时改同一块东西：

- 代码负责人：实现或整理退化生成方法，固定参数、随机种子和输出目录，保证能够重复生成相同结果。
- 质量负责人：查找真实场景依据，逐张检查生成结果，记录不真实、过强或已经无法辨认的图片，并整理实验对照表。

第一轮只用 20 张图片，覆盖运动模糊、低照度、遮挡、远距离缩小、压缩失真五类退化，每类先做轻、中、重三个等级。先把小样交给负责人看过，再扩大数据量或训练新模型。

做到这些就算这一轮完成：别人按记录能重现图片；参数记得清楚；原图没被改；训练、验证、测试没有混在一起；生成的图都有人看过。

## 平时怎么沟通

有进展时在群里说清楚三件事就行：这次做了什么，文件或代码放在哪里，还有什么拿不准。比如：“低照度轻中重三档已经生成，放在 `...`；中档有几张太黑，想请你看一下是否要调轻。”

如果发现图片效果不对、训练验证测试可能混了，或者觉得任务本身要改，就先说出来，别自己硬改到最后。
