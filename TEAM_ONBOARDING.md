# BirdPilot 新成员入门

这份说明面向第一次接触 BirdPilot 的团队成员。先理解项目、跑通小样，再领取完整数据或模型文件。

## 我们最终要做什么

BirdPilot 的目标是一台可放到野外的鸟类智能观测设备：摄像头取得图像，设备识别鸟种并保存观测记录。当前先在线完成模拟退化、模型对比和桌面设备闭环，真实野外观测放在最后验收。

当前已经有 Birds-525 上的 MobileNetV3 模型、ONNX 导出和 Mac 本地推理基准。尚未完成的核心工作是退化增强对照实验、RK3588S 板端推理和真实野外验证。

## 第一次拿到仓库后做什么

1. 阅读根目录 `README.md`，了解训练、导出和推理流程。
2. 创建 Python 环境并安装 `requirements.txt`。
3. 向项目负责人领取 `BirdPilot_starter_data_v1.zip`，不要索要或传播完整数据。
4. 解压后确认其中有 `train`、`valid`、`test` 三种划分、8 个鸟种和 `manifest.csv`。
5. 先查看图片、路径和标签，不要立刻修改模型或大批量生成退化图。
6. 按 `.agent/PLAN.md` 的 `Current Directive` 工作；不要同时扩展其他阶段。

## 数据边界

- `train`：未来可用于退化增强训练。
- `valid`：用于选择退化强度、概率和训练方案。
- `test`：只用于最终比较，禁止加入训练，也不能根据它反复调参数。
- 新收集的外部图片：后续作为额外盲测或真实野外证据，不能混写成现有测试集结果。

入门包只用于熟悉目录、检查图片和验证处理流程，不用于复现 524 类正式准确率。

## 模型文件如何领取

模型不放在公开 Git 仓库。通过项目负责人提供的团队共享目录领取，并保持以下文件成套对应：

- MobileNetV3 `.pt` 权重；
- 对应的训练配置；
- 524 类标签映射；
- 如果领取 ONNX，`.onnx` 与 `.onnx.data` 必须同时存在。

不要通过聊天软件反复转发模型副本。收到后记录文件名、版本和校验值。

## 两位新成员的第一项共同任务

你们共同交付一套“小规模模拟退化测试方案”，但各自承担不同责任：

- 代码负责人：实现或整理退化生成方法，固定参数、随机种子和输出目录，保证能够重复生成相同结果。
- 质量负责人：查找真实场景依据，逐张检查生成结果，记录不真实、过强或已经无法辨认的图片，并整理实验对照表。

第一轮只使用 20 张图片，覆盖运动模糊、低照度、遮挡、远距离缩小、压缩失真五类退化，每类先做轻、中、重三个等级。项目负责人验收之前，不扩大数据量、不训练新模型。

完成标准：别人能按记录重现图片；每个参数有记录；原图未被覆盖；训练、验证和测试边界未被破坏；质量负责人已经逐张检查。

## 如何汇报

每次检查只回答四件事：本轮要回答的问题、交付物在哪里、已经通过哪些检查、还有什么阻塞。不要用“差不多”“完成百分之多少”代替成果。

发现方案不现实、数据边界可能被破坏，或需要更改模型训练目标时，先暂停并联系项目负责人。
